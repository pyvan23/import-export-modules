# Usage:

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     its the table number that output            [number] [required]
  -e, --extend   number to limit number of the table      [number] [default: 10]
  -l, --list     list table                           [boolean] [default: false]
```